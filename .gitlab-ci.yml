image: node

before_script:
  - npm install

cache:
  paths:
    - node_modules/

pages:
  variables:
    DEPLOY_URL: "https://shujinarazaki.gitlab.io"
  script:
    - npm run update
    - npm run generate
  artifacts:
    paths:
      - dist
  only:
    - master
